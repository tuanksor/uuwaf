apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  namespace: uusec
  name: pod-lister
rules:
- apiGroups: [""]
  resources: ["pods"]
  verbs: ["list"]
- apiGroups: [""]
  resources: ["pods/exec"] # Thêm quyền cho subresource exec
  verbs: ["create"]
